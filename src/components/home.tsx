// src/components/Home.js
import React from 'react';

const Home = ({ title, content }) => {
  return (
    <div>
      <h1>{title}</h1>
      <div>
      </div>
    </div>
  );
};

export default Home;
